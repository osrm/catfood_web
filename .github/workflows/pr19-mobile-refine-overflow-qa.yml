name: PR19 mobile refine overflow QA

on:
  push:
    branches:
      - qa/pr19-mobile-refine-overflow-20260911

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-24.04
    env:
      VITE_SUPABASE_URL: https://gnosbstdatkytsyxuapt.supabase.co
      VITE_SUPABASE_PUBLISHABLE_KEY: sb_publishable_edpd7duWsG2uagRr-n3IHw_X1hEAjKV
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm
      - name: Test and build exact PR source
        run: npm ci && npm test && npm run build
      - name: Install Korean font
        run: sudo apt-get update -qq && sudo apt-get install -y --no-install-recommends fonts-noto-cjk
      - name: Run focused mobile refine overflow QA
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p qa-artifacts
          npm run preview -- --host 127.0.0.1 --port 4173 > /tmp/pr19-overflow-preview.log 2>&1 &
          PREVIEW_PID=$!
          trap 'kill $PREVIEW_PID 2>/dev/null || true' EXIT
          for i in $(seq 1 80); do curl -fsS http://127.0.0.1:4173/ >/dev/null && break; sleep 0.25; done
          node .github/pr19-mobile-refine-overflow-qa.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pr19-mobile-refine-overflow-qa
          path: qa-artifacts/
          if-no-files-found: error
